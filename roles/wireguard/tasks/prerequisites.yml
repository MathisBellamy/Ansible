---

- name: "prerequisites.yml | Check that mandatory variables are set"
  ansible.builtin.assert:
    that:
      - peer['pub_key'] is defined and peer['pub_key'] | length > 0
    fail_msg: "This role considers that the client's wireguard key have been generated by the client. Please generate it and put the associated public key"
    quiet: true
